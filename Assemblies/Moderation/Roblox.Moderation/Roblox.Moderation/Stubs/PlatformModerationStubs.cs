// Temporary stubs to unblock build. Replace with real references when available.
namespace Roblox.Platform.Moderation
{
    public interface IExpression
    {
        long Id { get; }
        string Value { get; }
    }
}
